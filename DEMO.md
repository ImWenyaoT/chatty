# DEMO

一份 5 分钟的演示脚本。每一步都给具体输入、预期现象，以及**这一步在证明什么**。

需要模型 API key。想验证的是工程结构而不是模型表现，看 [PROOF.md](PROOF.md)，那些全部离线可跑。

## 启动

```bash
pnpm install
cp .env.example .env      # 填 DEEPSEEK_API_KEY
pnpm dev
```

前端 http://localhost:5173，后端 http://127.0.0.1:8000。

演示用户在界面左上角切换：

| | 分群 |
| --- | --- |
| 用户 A | 活跃型 |
| 用户 B | 价格敏感型 |
| 用户 C | 高价值型 |
| 用户 D | 新客型 |
| 用户 E | 流失风险型 |

## 第 1 步：正常推荐（30 秒）

选 **用户 B**，输入：

```
请推荐 300 元以内的耳机
```

会返回若干耳机，每个带推荐理由与营销文案。

展开 trace，会看到五个步骤：`get_user_profile` → `search_products` → `check_inventory` →
`retrieve_knowledge` → `get_marketing_strategy`。

> **这一步证明**：前三步是 Harness 确定性执行的，模型没有决定权；后两步才是模型自己调的 Tool。
> 界面上显示的价格来自 SQLite 的最终重查，不是模型写的。

## 第 2 步：没有候选时不编（45 秒）

同一个用户，输入：

```
请推荐 200 元以内的耳机
```

不会返回商品，而是澄清，并且会说「当前条件下没有匹配商品」——**不会说缺货**。

> **这一步证明**：「没有候选商品」和「候选商品没有库存」是两件事，Harness 分得清。
> 模型即使想圆场也过不了 `validateClarificationEvidence`。演示时可以强调：
> 这类场景是推荐系统最容易出现幻觉的地方。

## 第 3 步：本轮需求覆盖历史画像（45 秒）

切到 **用户 A**（活跃型，历史偏好不是手机），输入：

```
请推荐 1500 元以内的手机
```

返回的是手机，不是历史偏好类目。

> **这一步证明**：画像是先验不是约束。本轮明确说了什么，就以本轮为准。

## 第 4 步：售罄商品不进推荐（45 秒）

切到 **用户 C**（高价值型），输入：

```
请推荐 3000 元以内的数码产品
```

返回结果里不会出现 P015——它在候选集里，但库存为零。

> **这一步证明**：库存过滤发生在推荐生成之前，而不是事后过滤。模型从一开始就没见过这个候选。

## 第 5 步：纯知识问答（30 秒）

```
你们使用哪家快递公司？
```

返回一段有依据的回答，trace 里只有 `retrieve_knowledge`，没有商品搜索和营销策略。

> **这一步证明**：Harness 按 TaskFrame 决定这一轮需要哪些步骤。纯知识问答不会浪费
> 商品搜索和营销策略的调用。

## 第 6 步：混合请求（60 秒）

```
推荐 300 元以内的耳机，并告诉我七天无理由退货条件
```

同时返回商品推荐和退货政策说明。trace 里 `retrieve_knowledge` 出现两次——一次
`scope="general"` 查政策，一次 `scope="product"` 查商品知识。

> **这一步证明**：一轮里可以同时存在商品需求和知识问题，两类知识分开检索、分开校验。
> 也是最能体现 Agentic RAG 的一步：检索次数和 query 由模型自己决定，但检索范围由 Harness 约束。

## 如果某一步失败了

**这是预期内的。** 端到端通过率实测在 0.571–0.857 之间波动，见
[PROOF.md 第 5 节](PROOF.md)。失败时界面会给出稳定错误码，而不是一段编造的回答——
**宁可失败，不返回没有事实支撑的内容**，这本身就是设计目标之一。

演示时如果碰上，建议直接讲这一点，比重试更有说服力。

## 想再深一层

- 架构与一次请求的完整链路：[README.md](README.md)
- 领域词汇与 Single Agent 判据：[CONTEXT.md](CONTEXT.md)
- 每条主张怎么自己验一遍：[PROOF.md](PROOF.md)
- 十章长文：[docs/chatty-book/](docs/chatty-book/)
